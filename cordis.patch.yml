# dsh-airp bundle layer.
# Installed via `dsh plugin --profile <name> add ./dsh-airp`.
- insert:
    - id: airp
      name: dsh-airp
      config:
        defaultPack: lotm-tingen
        loreBudgetChars: 4000
